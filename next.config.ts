import type { NextConfig } from "next";
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "source.unsplash.com",
      "images.unsplash.com",
      "ui-avatars.com",
      "res.cloudinary.com",
    ],
  },
};
export default nextConfig as NextConfig;
